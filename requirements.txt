# CIMENTA — dependencias de Python (entorno: cimenta_env, Python 3.12)
#
# El entorno se crea vacío. Las dependencias se irán agregando etapa por etapa
# conforme cada módulo (M1–M6) las necesite. Por ahora no hay dependencias fijadas.
#
# Para regenerar este archivo desde el entorno activo:
#   pip freeze > requirements.txt

# ---- M1 Ingesta ----
requests==2.34.2
PyYAML==6.0.3
tqdm==4.68.2

# ---- M1 · Fotos → Google Drive ----
google-api-python-client==2.197.0
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0

# ---- M1 · Portales tras Cloudflare (Nivel B) + geocoding ----
cloakbrowser==0.3.31      # Chromium stealth; resuelve Cloudflare Turnstile
beautifulsoup4==4.15.0    # parseo del DOM (tarjetas Inmuebles24)

# ---- M2 Valuación (AVM) ----
scikit-learn==1.9.0

# ---- App / visor (Streamlit + mapa OSM) ----
streamlit==1.58.0
pandas==3.0.3
folium==0.20.0
streamlit-folium==0.27.2
